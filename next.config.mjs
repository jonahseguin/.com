/** @type {import('next').NextConfig} */
const nextConfig = {
  /*async redirects() {
    return [
      {
        source: "/resume",
        destination: "https://jonahseguin.github.io/resume/resume.pdf",
        basePath: false,
        permanent: false,
      },
    ];
  },*/
};

export default nextConfig;
